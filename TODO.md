# Task: Apply primary color from settings to sidebar

## Steps

- [x] 1. Update `frontend/src/components/Sidebar.jsx` - Replace hardcoded blue/indigo gradient with a `sidebar-gradient` custom class
- [x] 2. Update `frontend/src/index.css` - Add CSS rules for `html[data-primary-color='green']` and `html[data-primary-color='purple']` to override the sidebar gradient to match the selected primary color
- [x] 3. Verify changes apply correctly for all primary colors (blue, green, purple)
